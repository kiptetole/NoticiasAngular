import { SubrayarDirective } from './subrayar.directive';

describe('SubrayarDirective', () => {
  it('should create an instance', () => {
    const directive = new SubrayarDirective();
    expect(directive).toBeTruthy();
  });
});
